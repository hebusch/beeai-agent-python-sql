---
# Ejemplo de Secret para credenciales de PostgreSQL
# 
# Para usar:
# 1. Copia este archivo: cp 04-postgres-secret.yaml.example 04-postgres-secret.yaml
# 2. Reemplaza los valores base64 con tus credenciales
# 3. Aplica: kubectl apply -f 04-postgres-secret.yaml
# 4. Descomenta las variables de entorno en 03-agent-deployment.yaml

apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
type: Opaque
data:
  # Valores en base64
  # Para generar: echo -n 'tu-valor' | base64
  
  # Usuario de PostgreSQL (ejemplo: postgres)
  username: cG9zdGdyZXM=  # postgres
  
  # Contraseña de PostgreSQL (REEMPLAZAR con tu password en base64)
  password: Y2FtYmlhbWU=  # cambiame
  
  # Host de PostgreSQL (ejemplo: postgresql.default.svc.cluster.local)
  host: cG9zdGdyZXNxbC5kZWZhdWx0LnN2Yy5jbHVzdGVyLmxvY2Fs  # postgresql.default.svc.cluster.local
  
  # Puerto de PostgreSQL (ejemplo: 5432)
  port: NTQzMg==  # 5432

